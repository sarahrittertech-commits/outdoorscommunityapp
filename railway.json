{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "HOSTNAME=0.0.0.0 npm start",
    "healthcheckPath": "/about",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}
